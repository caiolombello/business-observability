apiVersion: v1
kind: Service
metadata:
  name: {{ include "histograma.fullname" . }}
  labels:
    {{- include "histograma.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "histograma.selectorLabels" . | nindent 4 }}
